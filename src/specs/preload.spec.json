{
  "name": "Preload Bridge",
  "file": "src/preload/preload.ts",
  "description": "Electron preload script using contextBridge.exposeInMainWorld to expose window.electronAPI to the renderer process. Wires ipcRenderer.invoke (request/response) and ipcRenderer.send (fire-and-forget) for every channel. Registers event listeners for terminal:data, terminal:exit, file:changed — returns unsubscribe functions.",
  "type": "process",
  "layer": "foundation",
  "singleton": true,
  "exports": [],
  "dependencies": [],
  "referenced_by": [
    { "file": "src/global.d.ts", "usage": ["Window.electronAPI interface"] }
  ],
  "ipc": [],
  "interface": {
    "methods": []
  }
}
