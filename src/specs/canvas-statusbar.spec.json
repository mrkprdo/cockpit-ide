{
  "name": "Canvas Status Bar",
  "file": "src/renderer/components/canvas-statusbar.ts",
  "description": "Status bar UI showing zoom level, pan lock state, workspace path, and refresh/arrange controls. Provides interactive elements for zoom slider, lock toggle, and View All button.",
  "type": "ui",
  "layer": "core",
  "singleton": false,
  "exports": ["StatusBar"],
  "dependencies": [],
  "referenced_by": [
    { "file": "src/renderer/components/CanvasArea.ts", "usage": ["StatusBar"] }
  ],
  "ipc": [],
  "interface": {
    "constructor": "constructor(container: HTMLElement)",
    "methods": [
      "setZoom(zoom: number): void",
      "setWorkspace(path: string): void",
      "setLocked(locked: boolean): void",
      "onArrange(cb: () => void): void",
      "onViewAll(cb: () => void): void",
      "destroy(): void"
    ],
    "properties": ["element: HTMLElement"]
  }
}
