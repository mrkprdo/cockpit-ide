{
    "name":  "Terminal Plugin",
    "file":  "src/renderer/components/TerminalPlugin.ts",
    "description":  "xterm.js terminal emulator wrapped as a plugin card. Creates Terminal with Noir custom theme. Connects to node-pty via IPC (create/write/resize/kill). Clipboard paste (Ctrl+Shift+V, Ctrl+V). ResizeObserver-based fit. Keyboard event handling. Process exit detection.",
    "type":  "ui",
    "layer":  "plugin",
    "singleton":  false,
    "exports":  [
                    "TerminalPlugin"
                ],
    "dependencies":  [

                     ],
    "referenced_by":  [
                          {
                              "file":  "src/renderer/components/CanvasArea.ts",
                              "usage":  [
                                            "TerminalPlugin"
                                        ]
                          }
                      ],
    "ipc":  [
                "terminal:create",
                "terminal:write",
                "terminal:resize",
                "terminal:kill",
                "terminal:data",
                "terminal:exit",
                "clipboard:readText"
            ],
    "interface":  {
                      "constructor":  "constructor(container: HTMLElement, uuid: string, cwd?: string)",
                      "methods":  [
                                      "destroy(): void",
                                      "fit(): void",
                                      "updateTheme(): void"
                                  ],
                      "static_methods":  [
                                             "readTheme(): { background: string; foreground: string; cursor: string; selectionBackground: string }"
                                         ],
                      "properties":  [
                                         "uuid: string",
                                         "element: HTMLDivElement",
                                         "onExit: (() =\u003e void) | null"
                                     ]
                  }
}