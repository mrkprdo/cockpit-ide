{
  "name": "Monaco Editor Plugin",
  "file": "src/renderer/components/MonacoEditorPlugin.ts",
  "description": "Multi-tab code editor based on Monaco Editor (AMD-loaded). Manages a list of open file tabs with drag-and-drop reordering, close buttons, middle-click close, right-click context menu (Close/Close Others/Close All/Copy Path), dirty indicators, and language detection by extension (30+ languages mapped). Persists cursor positions and scroll positions per tab. Saves file contents on tab switch and auto-saves on external file changes. Exposes editor state (tabs, active file, cursors) for session persistence and AI agent access.",
  "type": "ui",
  "layer": "widget",
  "singleton": false,
  "exports": ["MonacoEditorPlugin"],
  "dependencies": [
    {
      "feature": "context-menu",
      "file": "src/renderer/components/ContextMenu.ts",
      "usage": "Opens right-click context menu on tabs with close/reorder/copy-path actions"
    },
    {
      "feature": "theme",
      "file": "src/renderer/theme.ts",
      "usage": "Reads theme colors (unused directly in this file but imported for potential editor theme integration)"
    }
  ],
  "referenced_by": [
    { "feature": "explorer-plugin", "file": "src/renderer/components/ExplorerPlugin.ts" },
    { "feature": "canvas-area", "file": "src/renderer/components/CanvasArea.ts" }
  ],
  "ipc": ["ide:editorState"],
  "interface": {
    "properties": [
      { "name": "tabs", "type": "Tab[]", "description": "Array of open file tabs with filePath, name, and originalPath" },
      { "name": "activeTab", "type": "string | null", "description": "Lowercased file path of the active tab" },
      { "name": "onStateChange", "type": "(() => void) | null", "description": "Callback when tabs or active file change" }
    ],
    "methods": [
      { "name": "constructor", "signature": "(container: HTMLElement)", "description": "Builds tab bar, editor area, inits Monaco with AMD loader, wires file change listener" },
      { "name": "openFile", "signature": "(filePath: string): Promise<void>", "description": "Opens file in a new tab or switches to existing tab; reads content from disk" },
      { "name": "switchTab", "signature": "(filePath: string): void", "description": "Saves current cursor/scroll, loads new tab content, restores cursor" },
      { "name": "closeActiveTab", "signature": "(): void", "description": "Closes the currently active tab" },
      { "name": "getContent", "signature": "(): string", "description": "Returns current editor content" },
      { "name": "insertText", "signature": "(text: string): void", "description": "Replaces current selection with text" },
      { "name": "getSelectionText", "signature": "(): string", "description": "Returns selected text" },
      { "name": "setContent", "signature": "(content: string): void", "description": "Replaces editor content (for AI agent)" },
      { "name": "goToLine", "signature": "(line: number, col?: number): void", "description": "Moves cursor to line/col" },
      { "name": "getState", "signature": "(): { openFiles: string[]; activeFile: string; cursors: Record<string, { lineNumber: number; column: number; scrollTop: number }> } | null", "description": "Serializable editor state for persistence" },
      { "name": "restoreState", "signature": "(state: ...): Promise<void>", "description": "Restores open tabs and cursor positions from saved state" },
      { "name": "reloadIfOpen", "signature": "(filePath: string): Promise<void>", "description": "Reloads file content from disk if tab is open (triggered by external file changes)" }
    ]
  },
  "lifecycle": {
    "created_by": "ExplorerPlugin constructor",
    "destroyed_by": "Card removal via CanvasArea",
    "singleton": false
  },
  "external_deps": ["monaco-editor (AMD-loaded from vs/)"]
}
