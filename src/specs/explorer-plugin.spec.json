{
  "name": "Explorer Split-Pane Plugin",
  "file": "src/renderer/components/ExplorerPlugin.ts",
  "description": "Combined split-pane plugin. FileExplorerPlugin (left, 260px default, resizable 120-600px) + MonacoEditorPlugin (right) in horizontal flex layout with drag handle. Bridges markdown openers and theme updates between child panes.",
  "type": "ui",
  "layer": "plugin",
  "singleton": false,
  "exports": ["ExplorerPlugin"],
  "dependencies": [
    { "file": "src/renderer/components/FileExplorerPlugin.ts", "usage": ["FileExplorerPlugin"] },
    { "file": "src/renderer/components/MonacoEditorPlugin.ts", "usage": ["MonacoEditorPlugin"] }
  ],
  "referenced_by": [
    { "file": "src/renderer/components/CanvasArea.ts", "usage": ["ExplorerPlugin"] }
  ],
  "ipc": [],
  "interface": {
    "constructor": "constructor(card: PluginCard, workspacePath: string, onStateChange?: () => void)",
    "methods": [
      "openFile(filePath: string): void",
      "closeActiveTab(): void",
      "openFileSearch(): void",
      "revealFile(filePath: string): void",
      "getEditorState(): EditorState | null",
      "restoreEditorState(state: EditorState | null): Promise<void>",
      "setMarkdownOpeners(labels: string[], callback: (filePath: string, label: string) => void): void",
      "updateTheme(): void"
    ],
    "properties": ["fileExplorer: FileExplorerPlugin", "editor: MonacoEditorPlugin"]
  }
}
