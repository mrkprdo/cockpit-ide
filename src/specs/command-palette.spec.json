{
  "name": "Command Palette",
  "file": "src/renderer/components/CommandPalette.ts",
  "description": "Ctrl+P file search overlay. Provides a search input that fuzzy-filters workspace files (excluding .git, node_modules, .cockpit) after loading them via fs.readDir. Results display file name and relative directory, with arrow-key navigation and Enter to open. Shows recent files (persisted in localStorage) when input is empty. Progress bar shown during file tree walk. Lazy-loaded — only created on first invocation.",
  "type": "ui",
  "layer": "overlay",
  "singleton": false,
  "exports": ["CommandPalette"],
  "dependencies": [],
  "referenced_by": [
    { "feature": "explorer-plugin", "file": "src/renderer/components/ExplorerPlugin.ts" }
  ],
  "ipc": ["fs:readDir"],
  "ui": {
    "spec": "command-palette-ui.spec.json"
  },
  "interface": {
    "methods": [
      { "name": "constructor", "signature": "(wsPath: string, onSelect: (filePath: string) => void)", "description": "Builds overlay, palette, input, results list, progress bar" },
      { "name": "open", "signature": "(): void", "description": "Shows overlay, focuses input, starts with recent files" },
      { "name": "close", "signature": "(): void", "description": "Hides overlay, clears search timer" },
      { "name": "destroy", "signature": "(): void", "description": "Removes overlay from DOM" }
    ]
  },
  "lifecycle": {
    "created_by": "ExplorerPlugin.openFileSearch() (lazy)",
    "destroyed_by": "Page unload (never explicitly destroyed)",
    "singleton": false
  }
}
