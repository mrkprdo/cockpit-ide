{
  "name": "Canvas Text Renderer",
  "file": "src/renderer/components/TextRenderer.ts",
  "description": "Canvas text utility wrapping @chenglou/pretext for measure(), draw(), createCanvas() (single-style), createCanvas2() (multi-style with varied line heights). Used by PluginCard for title rendering.",
  "type": "utility",
  "layer": "utility",
  "singleton": false,
  "exports": ["measure", "draw", "createCanvas", "createCanvas2"],
  "dependencies": [],
  "referenced_by": [
    { "file": "src/renderer/components/PluginCard.ts", "usage": ["measure", "createCanvas", "createCanvas2"] }
  ],
  "ipc": [],
  "interface": {
    "methods": [
      "measure(text: string, style: TextStyle): TextMetrics",
      "draw(ctx: CanvasRenderingContext2D, text: string, x: number, y: number, style: TextStyle): void",
      "createCanvas(text: string, style: TextStyle): HTMLCanvasElement",
      "createCanvas2(lines: {text: string, style: TextStyle}[]): HTMLCanvasElement"
    ]
  }
}
