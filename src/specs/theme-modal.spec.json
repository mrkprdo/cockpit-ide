{
  "name": "Theme Modal",
  "file": "src/renderer/components/ThemeModal.ts",
  "description": "Theme selection dialog allowing users to choose base theme (default/monokai/idol) and mode (dark/light). Provides preview swatches. Updates theme singleton on selection.",
  "type": "ui",
  "layer": "core",
  "singleton": false,
  "exports": ["ThemeModal"],
  "dependencies": [
    { "file": "src/renderer/theme.ts", "usage": ["theme", "themeConfigs"] }
  ],
  "referenced_by": [],
  "ipc": [],
  "interface": {
    "methods": ["open(): Promise<{base: string, mode: string} | null>", "close(): void"],
    "properties": ["overlay: HTMLElement", "modal: HTMLElement"]
  }
}
