{
  "name": "IDE Server",
  "file": "src/main/ide-server.ts",
  "description": "HTTP+WebSocket server for MCP/open-code protocol integration. Listens on a random localhost port. Broadcasts editor selection state changes to connected WebSocket clients via JSON-RPC 2.0 'selection_changed' notifications. Writes lock files to .cockpit/ide.lock, %USERPROFILE%/.claude/ide/{port}.lock, and XDG data path for tool discovery.",
  "type": "process",
  "layer": "foundation",
  "singleton": true,
  "exports": ["IdeServer", "EditorSelectionState", "ideServer"],
  "dependencies": [],
  "referenced_by": [
    {
      "feature": "main-process",
      "file": "src/main/main.ts"
    }
  ],
  "ipc": [],
  "interface": {
    "methods": [
      { "name": "start", "signature": "(wsPath: string): Promise<void>", "description": "Starts HTTP server + WebSocketServer on random port, writes lock files" },
      { "name": "stop", "signature": "(): void", "description": "Closes all client connections, stops server, removes lock files" },
      { "name": "updateEditorState", "signature": "(state: EditorSelectionState | null): void", "description": "Broadcasts selection_changed JSON-RPC notification to all connected clients" },
      { "name": "getPort", "signature": "(): number", "description": "Returns the listening port number" },
      { "name": "getLockPaths", "signature": "(): string[]", "description": "Returns the list of lock file paths written to disk" }
    ]
  },
  "lifecycle": {
    "created_by": "main.ts on workspace setPath",
    "destroyed_by": "main.ts on workspace close or app quit",
    "singleton": true
  },
  "external_deps": ["ws"],
  "test": "src/main/ide-server.test.ts"
}
